import * as LR from "@uploadcare/blocks";

LR.registerBlocks(LR);
export default function Home() {
  return (
    <main className="flex h-screen flex-col items-center justify-between">
      <div className="max-w-7xl border h-full w-full">
        <div>
        </div>
      </div>
    </main>
  );
}
